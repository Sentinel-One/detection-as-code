id: rule3-duplicate # rule file name does not have to be unique but the id must be unique
name: Duplicate Brute Force Login Attempt on Identity Provider
description: Detects multiple failed login attempts from a single source within a short time window
severity: Medium
expirationMode: Permanent
queryType: events
s1ql: 'event.type = "Login" AND event.login.loginIsSuccessful = false'
status: Draft
