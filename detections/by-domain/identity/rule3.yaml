id: rule3
name: Brute Force Login Attempt on Identity Provider
description: Detects multiple failed login attempts from a single source within a short time window
severity: Medium
expirationMode: Permanent
queryType: events
s1ql: 'EventType = "Login" AND LoginResult = "Failed"'
status: Draft
